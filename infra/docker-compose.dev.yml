services:
  api:
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    volumes:
      - ../apps/api:/app

  web:
    command: pnpm dev
    volumes:
      - ../apps/web:/app
      - /app/node_modules
      - /app/.next
